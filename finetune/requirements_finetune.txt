# LLM Fine-Tuning Requirements
# Tested with Python 3.10+ and CUDA 12.1
# GPU: 16 GB VRAM (e.g. RTX 4080, RTX A4000, RTX 3090)

# Core
torch>=2.3.0
transformers>=4.44.0
accelerate>=0.33.0
datasets>=2.20.0

# QLoRA / PEFT
peft>=0.12.0
bitsandbytes>=0.43.0
trl>=0.9.0

# Tokenization
sentencepiece>=0.2.0
protobuf>=4.25.0

# Utilities
tqdm>=4.66.0
loguru>=0.7.0
rich>=13.7.0

# Evaluation
scikit-learn>=1.5.0
numpy>=1.26.0

# Optional: Flash Attention 2 (faster training, requires Ampere+ GPU)
# pip install flash-attn --no-build-isolation
